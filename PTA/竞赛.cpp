#include <bits/stdc++.h>
#define IOS std::ios::sync_with_stdio(false)
using namespace std;
int main(int argc, char const *argv[])
{
    IOS;
    int flag=-1;
    while(true){
        if(!++flag){
            break;
        }
    }
    return 0;
}
